export let getters = {
  serviceWorker(state) {
    return state.serviceWorker;
  },
  serviceArea(state) {
    return state.serviceArea;
  },
  masterDetails(state) {
    return state.masterDetails;
  },
}
